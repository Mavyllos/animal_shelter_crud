<div class="container-fluid animals-cont">
    <a href="/animals/" class="all-animals">Return to Pets</a><br><br>
  <div class="row">
    <div class="col-md-2">
      <img src="{{ animal.image_url }}"
        alt="{{ animal.name }}"
        class="animal-image">
    </div>
  </div>
      <div class="row">
    <div class="col-md-6">
      <h2>Meet {{ animal.name }}!</h2>
      <p>
        <table class="table">
          <tbody>
              <tr>
                <th class="col-md-2"><b>Kind:</b> </th>
                  <td>{{ animal.kind }}</td>
              </tr>
              <tr>
                <th class="col-md-2"><b>Breed:</b> </th>
                  <td>{{ animal.breed }}</td>
              </tr>
              <tr>
                <th class="col-md-2"><b>Age:</b></th>
                  <td>{{ animal.age }}</td>
              </tr>
              <tr>
                <th class="col-md-2"><b>Description:</b></th>
                  <td>{{ animal.description }}</td>
              </tr>
            </tbody>
          </table>
        {{#if animal.is_adopted}}
        <hr>
          <p>Thanks for looking at my profile, but I have a home now.  Don't give up though, so many other pets need a home too!<p>
            {{else}}
            <hr>
        <form action="/animals/{{ animal.id }}?_method=PATCH" method="POST">
          <input class="btn btn-sm"
            type="submit"
            name="put"
            value="Adopt Me">
        </form>
        {{/if}}
      </p>
      <p>
        <form action="/animals/{{ animal.id }}/edit">
          <input class="btn btn-sm btn-link"
          type="submit"
          name="edit-button"
          value="Edit">
        </form>
        <form action="/animals/{{ animal.id }}?_method=DELETE" method="POST">
          <input class="btn btn-sm btn-link"
          type="submit"
          name="delete"
          value="Delete">
        </form>
      </p>
    </div>
  </div>
</div>
